algoritmo "conversor"
var
   angulo, seno: real
inicio
      escreva("Informe um ângulo: ")
      leia(angulo)
      seno <- sen(GraupRad(angulo))
      escreval("O seno de", angulo, " é", seno)
fimalgoritmo